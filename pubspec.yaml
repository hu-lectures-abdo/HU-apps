name: hulec

description: "A new Flutter project."

publish_to: 'none' # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  webview_flutter: ^4.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1  # Add this package

flutter:
  uses-material-design: true
  assets:
    - assets/1.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/2.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/2.png"
